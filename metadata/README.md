# Metadata Extraction

Extraction of implementation parameters used as metadata rather than architecture labels.

Metadata includes parameters such as:

- Bit width
- Reset mode
- Enable configuration
- Load behavior
- Direction
- Control configuration
